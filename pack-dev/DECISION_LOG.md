# Decision Log — AI Agent Starter Pack (pack development)
<!-- PACK-DEV ARTIFACT: this log tracks development of the pack itself.
     It is NOT a template. Do not copy it into deployed projects — agents
     create a fresh DECISION_LOG.md per project (see protocols/log-format.md). -->

## [2026-06-11] Audit of pack v11.51 — Claude Code
- Did: read-only audit (inventory, accuracy pass vs official docs, lifecycle map); no files changed
- Decisions: refactor approved on branch `revised` — WHY: always-on context ~16k tokens vs vendor 200-line guidance; broken security scan; CI false-green; missing lifecycle endpoints; AGENTS.md/CLAUDE.md source-of-truth inversion
- State: findings + 8-commit plan approved by user

## [2026-06-11] Fix broken gates and delete orphan configs (4110746) — Claude Code
- Did: protocols/sensitive-data.md scan rewritten (grep -E, single-`\` continuations, API-key formats); agent-ci.yml placeholder jobs exit 1; trufflehog pinned v3.95.5, base/head omitted, fetch-depth 0; .codex/config.toml fallback comment corrected; .gitignore added; root settings.json + config.toml deleted (orphans)
- Decisions: every gate must be demonstrated failing before trusted — WHY: shipped scan matched 0/4 planted secrets while reporting clean
- State: scan catches 4/4 planted secrets; unconfigured CI exits non-zero

## [2026-06-11] Split ARCHITECTURE.md into core + protocols (78d7394) — Claude Code
- Did: ARCHITECTURE.md 1,194→463 lines; new protocols: communication, log-format, safe-deletion, code-quality, environment; version-mismatch handler moved to edge-cases.md; protocol count 16→21
- State: all cross-referenced headings still resolve; zero broken paths

## [2026-06-11] AGENTS.md single source of truth (4df0b96) — Claude Code
- Did: AGENTS.md = Part 1 policy + Part 2 project specifics; CLAUDE.md → @AGENTS.md shim; ARCHITECTURE.md deleted, full reference sweep (zero dangling); Authority Matrix moved from README; ask-rules added to .claude/settings.json
- Decisions: Part 2 = bounded living summary (Pattern Registry ≤40 lines, Project-Specific Architecture ≤60) — WHY: always-on budget must not grow with project age. Audience Mode = always-on Part 2 field — WHY: non-dev mode must not depend on a trigger firing
- State: always-on ~7.6k tokens (was ~16k)
- Watch: .claude/settings.json ask-rule prompt could not fire mid-session (rules load at session start) — verify in a fresh Claude Code session

## [2026-06-11] OpenCode third harness + read-deny fix (2e09597, 1c1179f) — Claude Code
- Did: opencode.json (per-path edit-ask on pack files, read+edit deny on .env*/secrets/**, bash denies, webfetch allow); harness lists updated; permission-limits note in sensitive-data.md
- Decisions: opencode.json is defense-in-depth, not a boundary — WHY: bash-side reads (cat .env) bypass read-deny in all harness configs; real boundary = secrets out of repo
- Watch: opencode.json edit-ask prompt unverifiable from Claude Code — verify live in an OpenCode session

## [2026-06-11] Lifecycle endpoints + hardened deploy gate (088e6de, 61d4b8f) — Claude Code
- Did: protocols/product-definition.md (idea→brief→recommended stack→BACKLOG.md, item 1 = walking skeleton); protocols/run-demo.md (RUNBOOK.md + DoD "user has seen it run", full demo on backlog completion, user-only deferral); protocols/deployment.md (opt-in, two-signal escalate-only sensitivity gate, concrete verified teardown)
- Decisions: demo gate cannot be self-deferred by the agent — WHY: gate exists for the user's eyes, not the agent's confidence. Deploy gate combines user declaration + agent assessment, either escalates — WHY: false negative = privileged data on a public URL
- State: gate logic executed against test cases; all failure paths demonstrated

## [2026-06-11] Single trigger table (bba81ce) — Claude Code
- Did: PROTOCOLS.md deleted; AGENTS.md Protocol Index is the only table; edge-cases re-keyed; hardcoded counts replaced by ls-vs-index comparison (bidirectional: missing + orphan)
- Decisions: completeness check derives from the index, not a count — WHY: counts go stale; the index is the list
- State: 24 indexed = 24 on disk; check demonstrated failing both directions

## [2026-06-11] Breadcrumb mechanism: DECISION_LOG + HANDOFF (this commit) — Claude Code
- Did: protocols/log-format.md rewritten as the DECISION_LOG.md/HANDOFF.md spec (append-only + overwritten handoff); CHANGELOG.md retired (generate from log if needed); read order now AGENTS.md → HANDOFF.md → log tail; CAPTAINS_LOG migration path; full reference sweep across AGENTS.md, 10 protocols, SETUP, README, TASK_TEMPLATE; this file and HANDOFF.md created for pack development itself
- Decisions: append-only oldest-first — WHY: prepend rewrites the file top every task (merge conflicts, no cheap tail read); reconstruction and live entries now form one chronological stream
- Watch: known-limitations.md is currently indexed as a triggerable protocol and counted in the 24 — move to pack-dev space outside protocols/ in commit 7/8 and drop its index row
- Watch: DECISION_LOG.md + HANDOFF.md in the pack repo are pack-dev artifacts (like known-limitations.md) — exclude from deployed-project copies in the commit 7/8 distribution work

## [2026-06-11] Pack-dev boundary, lore strip, setup gaps (commit 7) — Claude Code
- Did: created pack-dev/ (known-limitations.md moved from protocols/, pack's own DECISION_LOG.md + HANDOFF.md moved from root, README explaining the boundary); known-limitations index row + standing rule dropped (23=23); SETUP gained Step 0 pack-acquisition placeholder, Node.js prerequisite, OpenCode install (verified: npm i -g opencode-ai / curl script), git-init-is-the-agent's-job note; AGENTS.md First Session step 1b (git init); Part 2 Language Rules block removed, Code Style reduced to deviations-only DEFERRED placeholder; placeholder-inference.md rewritten with three buckets (Required / Deferred / set-by-other-protocol) covering every Part 2 placeholder
- Decisions: distribution exclusion is a directory boundary (pack-dev/), not a file list — WHY: lists go stale as new pack-dev artifacts appear; same principle as ls-vs-index. Deployed projects create their OWN root DECISION_LOG/HANDOFF at first session — WHY: read-order paths are root-relative and must resolve per-project, never to pack history
- State: always-on = AGENTS.md 33.1k chars + CLAUDE.md shim ≈ 8.3k tokens (baseline ~16k); ls-vs-index PASS 23=23; zero known-limitations refs in agent-loaded files
- Watch: resolved this commit — known-limitations move, pack-dev distribution exclusion. Still open — the two enforcement probes (fresh-session .claude ask-rule; live OpenCode edit-ask)

## [2026-06-11] Consistency pass + v12.0 (commit 8, final build commit) — Claude Code
- Did: testing-strategy trigger aligned (index row + protocol header: "not reviewing results / running existing suite"); session-type-D wording verified already consistent post-merge (no change needed); README session-start line updated for three harnesses; .gitattributes added (* text=auto); sensitive-data.md scope line added (catches known formats only — privileged content must stay out of the repo; clean scan ≠ clearance); known-limitations ledger verified (platform row updated for OpenCode; version-header row resolved — root files standard header, protocol files keep file-identifying variant intentionally; TASK_TEMPLATE.md header added); all headers bumped v11.51 → v12.0 (2026-06-11)
- Decisions: protocol files keep the file-identifying header variant — WHY: preserves provenance when a protocol is pasted into a chat in isolation
- State: self-checks PASS — version grep (all v12.0), ls-vs-index (23=23), dead-link scan (only intentional migration mentions), referenced paths exist, JSON+YAML parse. Refactor build phase complete: 12 commits on `revised`
- Watch: carried OPEN, not closed — (1) .claude/settings.json ask-rule fresh-session probe, (2) opencode.json edit-ask live probe, (3) SETUP Step 0 distribution-point placeholder (user fills). Next phase: effectiveness trials

## [2026-06-11] W1: enforcement-tooling protocol (wave 2) — Claude Code
- Did: protocols/enforcement-tooling.md (six-gate bundle: strict lint, format, type check, import-boundary rules derived from the Part 2 architecture sketch, pre-commit secret hook, real CI commands replacing exit-1 placeholders; one bundled confirmation; mandatory verify-each-gate-can-fail step; strictness rules — loosening is a logged decision, suppressions need justification; baseline/ratchet mode for inherited codebases); wired into product-definition (backlog item 1 + done criteria), inherited-codebase Phase 3 step 4b, AGENTS.md standing rule + index row, agent-ci.yml setup comment
- Decisions: tooling setup is the agent's job at walking-skeleton time — WHY: prose rules are probabilistic, tooling is the model-independent layer, and a non-dev will never configure CI; gates exist before the first feature. Verify-can-fail is mandatory in the protocol itself — WHY: a gate that has never failed proves nothing (audit finding A4/A6 class).
- State: live gate demo in scratch project — ruff failed on planted unused import (2 errors), import-linter broke the layers contract on planted data→controllers import then KEPT after fix, pre-commit hook blocked a staged AKIA-format key then allowed the clean commit. All three failed and passed. Protocols 23→24, ls-vs-index PASS.

## [2026-06-11] W2: secure-coding protocol + threat model + SAST (wave 2) — Claude Code
- Did: protocols/secure-coding.md (boundary input validation, parameterized-only queries, authz-on-every-endpoint incl. IDOR ownership checks, framework auth/sessions never hand-rolled, output encoding/CSRF, path/upload safety, error hygiene, recorded per-task self-check, escalate-to-humans rule for regulated domains); product-definition gained a data question + Data & trust threat-model block in the brief (feeds secure-coding scope and pre-answers the deployment gate); DoD security item; AGENTS.md standing rule + index row; enforcement-tooling bundle row 6 = SAST; agent-ci.yml security job gained a working semgrep step (--config auto --error)
- Decisions: SAST default is semgrep with auto config — WHY: stack-agnostic, works unconfigured in ubuntu CI; agent refines rulesets at enforcement-tooling time. Regulated-domain escalation stated in-protocol — WHY: the pack raises the floor, it must not imply certification.
- State: checklist gate demo caught 3/3 planted vulns (string-built SQL, IDOR, XSS) and passed the clean version. HONEST FINDING: semgrep on the Windows dev host runs as a SILENT NO-OP (no rules executed, exit 0) — a live instance of the silent-false-negative class; the protocol's verify-can-fail step catches exactly this. Local SAST therefore NOT claimed; CI (ubuntu) is the real gate. Protocols 24→25, ls-vs-index PASS, CI YAML parses.
- Watch: PROBE 3 — verify the semgrep CI step on the first real push to GitHub Actions (plant a string-built query in a scratch branch; the security job must fail).

## [2026-06-11] W3: architecture-fit at day one (wave 2) — Claude Code
- Did: product-definition Step 3b — S1–S4 sizing ladder (single-file tool → client+server+DB), one WHY per layer, sketch + Key Invariants written into Part 2 on day one; named growth triggers (auth, second entity, second consumer, ~300-line file) each forcing a logged decision; done-criteria line. code-quality.md layer table subordinated to the sketch (both directions: no manufactured layers, no skipped mandated layers). AGENTS.md Pre-Edit 6b conformance check; Part 2 architecture comment rewritten (day-one, growth-trigger governed) + Size & Structure template. placeholder-inference: architecture/invariants moved from Deferred to set-by-other-protocol.
- Decisions: structure is decided at the lowest rung that fits and revised only via logged triggers — WHY: prevents both failure modes (ceremony on a 200-line tool; multi-user app with no logic/data layer) and silent drift between them; the sketch's invariants double as the import-boundary contracts so W1's row 4 enforces what Step 3b decides.
- State: rubric gate demo — todo script sized S2, multi-user app S4; over-engineering flagged (S1 given controllers/services), under-engineering flagged (S4 with UI writing DB directly: missing service+data); conformant S4 passes; triggers fire on auth/second-entity/second-consumer, silent on cosmetic rename. ls-vs-index PASS (25).

## [2026-06-11] W4: independent review pass (wave 2) — Claude Code
- Did: protocols/review.md (fresh-context review on backlog-item completion before the full demo + before any deploy; four dimensions: correctness/security/architecture/readability; harness-aware mechanism — Claude Code subagent given ONLY diff+sketch+checklist, portable diff-only fallback for Codex/OpenCode requiring every verdict to cite file:line; verdict format with BLOCKER/minor + mandatory could-not-verify list; closure rules; no-self-waiver). Wired: run-demo gate step 1 (review THEN demo), deployment Step 0b, DoD line, standing rule, index row.
- Decisions: review is item-level not per-task — WHY: per-task review doubles all costs for marginal gain; per-item matches the demo cadence. Verdicts must cite artifacts (file:line) — WHY: a verdict that cannot cite the diff is the author's memory talking. Could-not-verify list mandatory — WHY: silence about limits reads as coverage (same class as silent-scan false negatives).
- State: LIVE gate demo — a real fresh-context subagent (no session context) reviewed a planted-flaw diff and caught all 6 planted blockers (incomplete item, string-built SQL, IDOR, XSS, ui→data layer violation, missing error paths) + 2 minors, and honestly listed could-not-verify including possible mitigating middleware. Closure logic: 6 blockers → demo BLOCKED; author waiving a blocker → INVALID; fixed+re-reviewed → gate opens; missing CNV list → BLOCKED. Protocols 25→26, ls-vs-index PASS.

## [2026-06-11] W5: scrutiny baselines + v12.1 (wave 2 complete) — Claude Code
- Did: a11y baseline in code-quality.md (semantic elements, alt text, keyboard reachability, contrast, titles) + keyboard-only pass in the run-demo full demo; .github/dependabot.yml template (ecosystem set during enforcement-tooling — wrong value is a silent no-op, so assignment lives in bundle row 7); license check at dependency-add (copyleft/unknown → confirm) + update cadence in external-research; measured coverage floor for critical paths in testing-strategy (floor failure fails DoD, lowering is a logged decision); production observability in environment.md + "when something breaks" line in the deployed-version runbook block; headers bumped v12.0 → v12.1
- Decisions: a11y folded into code-quality + demo gate rather than a new protocol — WHY: it is a property of UI code and of the demo, not a separate workflow; avoids protocol sprawl. Coverage floor scoped to critical-path modules, not global — WHY: global numbers are vanity metrics; auth/payments/data-writes are where misses cost.
- State: wave 2 complete (W1 enforcement tooling, W2 secure coding+SAST, W3 day-one architecture sizing, W4 independent review, W5 baselines). Self-checks all PASS: version grep v12.1, ls-vs-index 26=26, dead-link scan clean, paths exist, JSON+YAML parse. 17 commits on `revised`.
- Watch: carried OPEN — PROBE 1 (.claude ask-rule, fresh session), PROBE 2 (opencode.json live-fire), PROBE 3 (semgrep CI on first push), SETUP Step 0 distribution link. Next: effectiveness trials.

## [2026-06-11] PROBE 1 result: .claude ask-rule VERIFIED — Claude Code (fresh session, user-run)
- Did: fresh Claude Code session asked to edit protocols/stuck-loop.md; the Edit(protocols/**) ask rule fired — diff preview + Yes/No/don't-ask prompt appeared BEFORE execution; edit never landed (working tree clean). Probe 1 PASSED.
- Decisions: probe initially misread as failed because the MODEL agreed to attempt the edit — that is correct pack behavior (explicit user instruction is the legitimate pack-edit path); the harness prompt is the backstop layer and it held. Recorded so future probe-readers measure the harness layer, not the model's willingness.
- State: enforcement layers verified 1 of 3 (.claude ask-rule live-fired). Remaining: PROBE 2 (opencode.json live-fire), PROBE 3 (semgrep CI on first push).

## [2026-06-15] Model-tiering policy for sub-agents + v12.2 — Claude Code
- Brief (confirmed): cheaper/faster models for bounded, rule-bound watch/governance sub-agent tasks; judgment + safety-critical work stays on the main model and is never downgraded; bake the selection rule into pack standing rules so deployed projects inherit it. User decisions: model-agnostic tiers with current models as examples (not pinned); Light-tier routing is a silent default but logged.
- Did: new protocols/model-tiering.md (three tiers — Deterministic/no-LLM, Light/cheap, Capable/main; three-part selection gate (rubric-driven + mechanically-checkable + caught-downstream); never-downgraded hard list = review, secure-coding, conflict resolution, architecture conformance, guardrail calls; record-the-tier honesty rule; harness-aware mechanism for Claude Code / OpenCode / Codex with fail-safe "can't route → run Capable, never skip"). Wired: AGENTS.md standing rule + Protocol Index row (26→27 protocols); review.md annotated (reviewer is Capable-tier, never downgraded). All headers bumped v12.1→v12.2 (2026-06-15).
- Decisions: tier by task property, not model brand — WHY: model IDs drift and differ across the three harnesses; pinning rots. Reviewer/secure-coding never downgrade — WHY: a cheap watchdog that misses a blocker defeats the gate's whole purpose. Gate part (c) cannot be satisfied by a human demo alone for guardrail-adjacent work — WHY (review finding): a non-dev demo is not a defect backstop. Light routing changes only which model runs a downstream-checked scan, never what work is done or which guardrails apply — WHY (review finding): "silent default" must not read as license to self-delegate guardrail-adjacent work.
- External research (verified, sourced): Claude Code `model:` frontmatter (haiku/sonnet/opus/inherit) + Agent-tool model param + CLAUDE_CODE_SUBAGENT_MODEL; OpenCode `agent.<name>.model` "provider/model-id"; Codex `.codex/agents/*.toml` model + model_reasoning_effort (caveat openai/codex#15250). All three default to inherit when unset.
- Review: independent fresh-context reviewer (Capable tier per the new rule), artifacts-only — verdict APPROVE, 0 blockers, 4 minors; 3 fixed (gate-(c) tightening, silent-default tie-back, Codex issue-number dated/caveated), 1 fixed (index-row wording aligned to file header). No findings carried as watch items.
- State: self-checks PASS — version grep v12.2 (AGENTS+CLAUDE), no stray v12.1 outside pack-dev, ls-vs-index 27=27 bidirectional, all cross-refs resolve. 19 commits on `revised` (after this commit).
- Watch: carried OPEN, not closed — PROBE 2 (opencode.json live-fire, needs OpenCode session), PROBE 3 (semgrep CI on first push), SETUP Step 0 distribution link. New: model-tiering is prose-verified only — no live cheap-vs-capable sub-agent run has been dogfooded in a deployed project yet (trial-phase candidate).

## [2026-06-15] Provider-agnostic tiers + per-project tier map + v12.3 — Claude Code
- Brief (confirmed): make model-tiering work with any provider/environment (not just Anthropic/Claude); add a setup-time provision that establishes each project's concrete tier map and how to switch tiers. User decision: standard setup step with a safe single-tier default (detect provider, propose Light+Capable, ask once; skip → everything Capable; switchable later).
- Did: model-tiering.md reworked provider-neutral — separated the two axes (PROVIDER = which models exist; HARNESS = the knob that switches); new "Tier map (per project)" section (roles are universal, models are project-specific, stored in Part 2; single-tier always valid; absent/unknown config degrades to Capable, never skip/guess); "The switch — harness mechanism" now states the knob takes whatever provider/model string the map holds (openai/…, google/…, ollama/…) + an "Other/SDK" row for any per-call model param. New AGENTS.md Part 2 "Model Tiers" section (bounded: provider line + 3-row role→model→how-to-switch table; single-tier = Light row "none"). Wired: product-definition Step 3c + done-criteria line; inherited-codebase Phase 3 step 4c; placeholder-inference registers Model Tiers as set-by-stack-selection. Headers v12.2→v12.3.
- Decisions: roles are provider-neutral capability slots; concrete models live only in Part 2 (no duplication) — WHY: model IDs and providers differ per project and drift; the protocol must not hardcode them. Fail-safe is structural, stated 3× — WHY: under an arbitrary/unknown provider the only safe degradation is "run on the main model," never "skip" or "guess cheaper." Single-tier is the default, not a missing-config error — WHY: most projects have one model; tiering is an opt-in cost lever, never a requirement.
- Review: independent fresh-context reviewer (Capable tier), artifacts-only — verdict APPROVE, 0 blockers, 3 cosmetic minors; 2 fixed (dropped redundant Part 2 "Mode" field that could drift out of sync with the Light-row status; added template note to fill "How to switch" with only the harness in use), 1 no-action (naming verified already consistent).
- State: self-checks PASS — version grep v12.3, no stray v12.2 outside pack-dev, ls-vs-index 27=27, all cross-refs resolve, step numbering 3→3b→3c→4 intact. 20 commits on `revised` (after this commit).
- Watch: carried OPEN — PROBE 2 (opencode.json live-fire), PROBE 3 (semgrep CI on first push), SETUP Step 0 distribution link, model-tiering live dogfood (now also: exercise a non-Anthropic tier map end-to-end).

## [2026-06-15] Pass 1: lossless always-on slim for small/local LLMs + v12.4 — Claude Code
- Context: pack must run on small/local models (target: quantized 12B on RTX 3070 8GB + 64GB RAM → context is the bottleneck, ~8-16k usable; instruction-following limited). Decided (with user): keep full functionality, do a single lean AGENTS.md (no second entrypoint, no RAG — the Protocol Index already IS a zero-infra lazy-loader; harness auto-compact isn't ours to build, the HANDOFF/log breadcrumb system is our equivalent). Key fact established: session control can't shrink the always-on FLOOR (loaded before any work) — only restructuring can. This is Pass 1 of 2; Pass 2 = user-selectable FULL/LEAN profile + lean Part 2.
- Did: moved detailed checklists OUT of always-on into two new on-demand protocols — session-start.md (First Session + Resumption checklists) and task-workflow.md (Pre-Edit 9-step + checkpoint/rollback). AGENTS.md keeps always-on: read order, meta-review preemption, session-type router, version check, Task Brief summary, Scope Control, Cross-Cutting trigger, full Definition of Done, hard guardrails (VERBATIM, untouched). Compressed Standing Rules to one line each and the Protocol Index When-to-load cells. Repointed 4 index rows (First Session/Resumption→session-start, Pre-Edit→task-workflow). Headers v12.3→v12.4.
- Invariant held: every policy rule RELOCATED, never removed or weakened — behavior unchanged on a capable model (lossless). Independent fresh-context review (Capable tier) verified step-by-step: APPROVE, 0 blockers, 0 behavior-affecting findings.
- Decisions: no embedding-RAG — WHY: breaks tri-harness drop-in portability, adds a vector-store/retrieval dependency a local box can't spare, and duplicates what the trigger table already does. Single lean AGENTS.md not a second CORE.md — WHY: anti-duplication + no build step; the floor reshapes to CORE shape (guardrails + router + DoD + standing rules + index) without a second artifact. Guardrails + DoD stay verbatim always-on — WHY: safety content must be resident, especially for weak models that follow only what they can see.
- State: floor ~9226t → ~8254t (~11%, measured). Part 1 6741t → ~6168t. Honest finding: floor is now dominated by irreducible always-on (guardrails ~1.4k, Part 2 specifics ~2k, index ~1.2k) — the bigger local-specific cut is Pass 2 (LEAN Part 2 + selective protocol loading). Self-checks PASS: version v12.4, no stray v12.3 outside pack-dev, ls-vs-index 29=29 bidirectional, all cross-refs resolve, completeness verified (all 17 standing-rule pointers + every moved step present). Protocols 27→29.
- Watch: carried OPEN — PROBE 2/3, SETUP Step 0 link, tiering live dogfood (incl. non-Anthropic map). NEW: Pass 2 pending — FULL/LEAN profile in Part 2 tier map (pack profile + context budget), session-start router reads it, checkpoint cadence scales to context window, LEAN Part 2 variant. Indirection risk to watch: weak models must reliably follow "load protocol X" pointers — validate in the local dogfood.

## [2026-06-15] Pass 2: user-selectable FULL/LEAN pack profile + v12.5 — Claude Code
- Pre-check: Haiku-proxy indirection test (dogfooding model-tiering) — a weaker model, given only the slimmed floor, correctly routed all 3 scenarios to the externalized protocols (session-start.md, product-definition.md cascade, task-workflow.md + conditional secure-coding/external-research) and explicitly recognized it must OPEN the protocol rather than wing the checklist. De-risks Pass 1's pointer-indirection; caveat logged (Haiku > quantized-12B, necessary-not-sufficient; real-rig test still pending).
- Did: added FULL/LEAN pack profile. context-window.md gains a "Pack profile" section (FULL = default, load triggered protocols normally, checkpoint at 5; LEAN ≤~16k = load only strictly-needed protocols, checkpoint at 2-3, collapse Part 2 architecture+Pattern Registry to one-line DECISION_LOG pointers) + both checkpoint thresholds made profile-aware. AGENTS.md Part 2 → Model Tiers gains Pack profile + Context budget fields (kept section name to preserve cross-refs); Session Start gains an always-on profile-read note; bounded-summary rule + Pattern Registry + Architecture comments gain the LEAN-collapse note. product-definition Step 3c + inherited 4c + placeholder-inference now set the profile alongside the tier map. model-tiering.md cross-refs the profile. Headers v12.4→v12.5.
- Decisions: profile behavior lives in context-window.md — WHY: it's a context-budget posture (cadence + footprint), the same concern that protocol already owns; folded in rather than a new protocol (no ls-vs-index growth). LEAN trims footprint, NEVER a gate — WHY: safety must be model/context-independent (same principle as model-tiering "never downgraded"); the no-relaxation invariant is restated everywhere LEAN appears. FULL is the unset default at every consumer.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers, 2 minors, both FIXED: the always-on AGENTS.md LEAN note now carries the "Protocol Index router still fires for every safety-critical trigger" bound (was only in the on-demand context-window.md, which isn't resident until 2-3 tasks in) and names secure-coding in the no-relaxation list (consistency with context-window.md).
- State: floor (FULL template) ~8254→~8346t (profile machinery adds a touch); LEAN realizes ~700-900t off a REAL project once architecture/patterns are filled and collapse to pointers, plus fewer protocols loaded per session + tighter checkpointing. Self-checks PASS: version v12.5, no stray v12.4 outside pack-dev, ls-vs-index 29=29, all cross-refs resolve, FULL-default verified at all consumers, both checkpoint thresholds agree. Lightweight-for-local work complete (Pass 1 lossless slim + Pass 2 profile).
- Watch: carried OPEN — PROBE 2/3, SETUP Step 0 link, model-tiering live dogfood (incl. non-Anthropic map). KEY VALIDATION still pending (no rig access this session): run the full pack on a real quantized 12B at 8-16k LEAN — confirm (a) pointer-indirection holds on a 12B (Haiku passed as proxy), (b) the LEAN floor actually fits, (c) safety triggers still fire. Session at ~5 tasks → checkpoint per context-window.md; fresh session recommended next.

## [2026-06-15] protocols/upgrade.md — pack upgrade/migration protocol + v12.6 — Claude Code
- Brief (confirmed): build protocols/upgrade.md, a procedure (not a script) to migrate an existing deployed project from an older pack version to a newer one. User confirmed: bump to v12.6, procedure-only.
- Did: new protocols/upgrade.md (file-ownership table separating pack-owned/replace-wholesale from project-owned/never-touch; 7 ordered steps — determine source+target version from AGENTS.md header / CAPTAINS_LOG for pre-v12.0, branch pack-upgrade/vX.Y, splice AGENTS.md preserving project Part 2 verbatim + add new Part 2 sections as NOT-SET placeholders by ##-header reconcile, diff-and-confirm enforcement/CI configs, apply version-to-version structural deltas, self-checks, review+diff+handoff; fail-safe summary). Wired: AGENTS.md standing-rule line + Protocol Index row (29→30 protocols), edge-cases.md version-mismatch handler gains a "to migrate rather than halt, see upgrade.md" pointer. All pack headers bumped v12.5→v12.6 (34 protocol/root headers + README).
- Decisions: procedure not script — WHY (user): every diff-and-confirm step exists because the file may be user-customized; automation would skip the human gate. Ownership is a table, project-owned default — WHY: a file not on the list is left alone and flagged, never assumed pack-owned (same fail-safe shape as ls-vs-index/distribution boundary). README.md is project-owned in a deployed project (the project replaced the pack's) — WHY: blindly overwriting it would destroy the project's own readme. AGENTS.md Part1/Part2 splice keyed on the "# Part 2" heading with stop-and-ask if the boundary is unclear — WHY: a wrong cut either drops project content or leaks stale policy.
- Review: independent fresh-context reviewer (Capable tier, artifacts-only) — APPROVE-WITH-MINORS, 0 blockers, 7 minors. Fixed 5: (#1) .github/*.yml glob missed .github/workflows/agent-ci.yml → .github/**/*.yml + explicit paths; (#4) Step 6 version-grep widened to protocols/+TASK_TEMPLATE+SETUP to actually verify "every header matches"; (#5) pre-v12.0 content-carry now points to the bounded-summary rule + DECISION_LOG overflow (line caps); (#6) renamed-vs-removed Part 2 section now flagged as possible rename instead of silently splitting; (#7) Step 7 review reworded as reuse of the review.md mechanism (pack upgrade isn't a literal review.md trigger). No-action 2: SETUP.md framing and README pack-owned caveat already mitigated by the per-change confirm gate and the "in a deployed project" scope already in the cell.
- State: self-checks PASS — version grep all v12.6 (no stray v12.5 outside pack-dev), ls-vs-index 30=30 bidirectional, upgrade.md cross-refs (edge-cases/log-format/review) resolve. Procedure-only as scoped; no executable.
- Watch: carried OPEN — KEY VALIDATION (real quantized-12B LEAN run), PROBE 2 (opencode.json live-fire), PROBE 3 (semgrep CI on first push), SETUP Step 0 distribution link, model-tiering live dogfood (incl. non-Anthropic map). NEW: upgrade.md is prose-verified only — no end-to-end migration has been dry-run against a real older-version deployed project (esp. the pre-v12.0 CAPTAINS_LOG/ARCHITECTURE/PROTOCOLS delta path). Dogfood candidate when an older-version project is available.

## [2026-06-15] Correct per-harness tiering mechanism + blank-tier-map fill rule + v12.7 — Claude Code
- Trigger: user reported their live OpenCode instance says it "depends on opencode configuration" and exposes "no per-call model selector" — contradicting the pack's framing that all three harnesses share one per-delegation model knob.
- External research (verified against official docs, External-Research Protocol): opencode.ai/docs/agents, developers.openai.com/codex/subagents, code.claude.com/docs/en/sub-agents. Finding: TWO mechanisms, not one. Per-DEFINITION model (pin a model in a named subagent's definition) works in all three. Per-CALL dynamic model selection at spawn time exists ONLY in Claude Code (Agent/Task tool model param; resolution order env CLAUDE_CODE_SUBAGENT_MODEL → per-call → frontmatter → main). OpenCode and Codex have NO per-call selector (OpenCode issue #6651 open FR; Codex per-agent TOML only). OpenCode gotcha: a subagent in opencode.json can be mis-treated as primary, ignoring mode:subagent (issue #22130) → prefer .opencode/agent/*.md. Codex: subagents now a documented feature (.codex/agents/*.toml model + model_reasoning_effort); only missing a global default-subagent-model (#19482) — the pack's old "rough edges, codex#15250" caveat was stale.
- Did: rewrote model-tiering.md "The switch — harness mechanism" around the per-definition (portable) vs per-call (Claude-Code-only) distinction + the portable rule "predefine the tier agents up front." Corrected per-harness blocks (OpenCode per-definition-only + #22130 markdown-file preference + #6651; Codex documented TOML + #19482; Claude Code both paths w/ full resolution order). Updated AGENTS.md Part 2 Model Tiers "How to switch" cells (.opencode/agent/*.md not opencode.json; per-call = Claude-only). Headers v12.6→v12.7.
- Also (user follow-up): added a "never leave the block silently blank" rule — when a session finds Part 2 Model Tiers unset and needs it (new project, inherited onboarding, or just after an upgrade that added it NOT-SET), fill it then (detect provider, propose Light+Capable, ask once; single model → single-tier auto; declines → single-tier explicit). Wired the same into upgrade.md: Step 3 reconcile note (SETUP sections are NOT-SET but filled via prompt, reconciling "never invent" with "don't leave blank") + a Step 7 close gate.
- Decisions: rest the negative capability claim ("no per-call outside Claude Code") on per-call being documented for Claude Code and ABSENT from the OpenCode/Codex docs — WHY: correct epistemic standard for "feature does not exist." "Never invent values" (upgrade.md) ≠ "leave a needed setup block blank" — WHY (user): a NOT-SET tier map silently defeats tiering; prompt-to-fill resolves it without guessing.
- Review: independent fresh-context reviewer (Capable tier) RE-FETCHED all three official docs and verified every factual claim incl. the load-bearing one — APPROVE, 0 blockers, 2 cosmetic minors (one a misread of an already-correct "/"; issue numbers supplementary). The blank-fill additions are additive/consistent with the reviewed direction and the existing setup flow; self-verified for ordering (Step 6 NOT-SET check precedes Step 7 fill gate — no contradiction).
- State: self-checks PASS — version v12.7 (no stray v12.6 outside pack-dev), ls-vs-index 30=30 bidirectional, cross-refs resolve. model-tiering.md + upgrade.md + AGENTS.md Part 2 mutually consistent.
- Watch: carried OPEN — KEY VALIDATION (real-12B LEAN), upgrade.md end-to-end dry-run, PROBE 2/3, SETUP Step 0, model-tiering live dogfood. The dogfood should now exercise a PREDEFINED light-tier subagent in OpenCode/Codex (not a per-call override) to confirm the corrected mechanism works in practice.

## [2026-06-15] Pack update-check (detect layer) + opt-in launch notify-hook + v12.8 — Claude Code
- Brief (confirmed, cross-cutting pre-flight approved): add an "update checker" — detect-only, reusing the existing upgrade.md for apply. User picked scope "on-demand check + Claude-Code launch notify-hook (notify-only)" over mandatory-launch-auto-download.
- External research (verified): code.claude.com/docs/en/hooks — SessionStart (matcher "startup") runs a command at launch; stdout is injected as session context; settings.json shape {hooks:{SessionStart:[{matcher,hooks:[{type:command,command}]}]}}. Confirmed before writing the hook.
- Did: new protocols/update-check.md (portable on-demand check: read local AGENTS.md version, fetch recorded Pack source URL, compare vX.Y, offline/no-source/no-fetch-tool → SKIP silently, behind → hand to upgrade.md; never auto-applies; honesty rule = report versions read, never imply up-to-date from a failed fetch) + documents the opt-in hook. New .claude/hooks/check-pack-update.sh (notify-only SessionStart script: prints one line only when upstream is strictly newer, exits 0 on every failure, silent no-op when PACK_SOURCE_URL unset = default; curl/wget with 5s timeout + --max-filesize 1MB; reads only a version string, never executes fetched content). Wired: AGENTS.md standing-rule line + Protocol Index row (30→31) + Part 2 Related-Docs "Pack source" NOT-SET row; upgrade.md intro note (update-check is the read-only detect front-end) + a new caveat that section-level Part 2 reconciliation misses new in-section rows/fields; SETUP Step 0 reframed to "optionally record the pack source to enable update checks" (closes the long-open Step 0 distribution-link watch item's intent). Headers v12.7→v12.8.
- Decisions: detect and apply are separate protocols — WHY: checking is cheap/read-only/anytime; applying replaces pack files and must stay gated+branched+confirmed (hard guardrail: pack files change only on explicit instruction). Launch hook is OPT-IN, OFF by default, Claude-Code-only — WHY: a mandatory per-launch network ping breaks offline/local-LLM-first and tri-harness parity; the portable on-demand check works everywhere, only the convenience trigger is harness-specific. No auto-download/auto-apply at launch — WHY: supply-chain trust (the pack governs the agent; letting a URL rewrite guardrails unattended is the thing to avoid) + the hard guardrail.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE-WITH-MINORS, 0 blockers; ran the version arithmetic incl. multi-digit (12.10 vs 12.9) and confirmed no write/apply/non-zero-exit path, no injection, default no-op. Fixed 4 minors: curl --max-filesize 1MB bound (unbounded-body could-not-verify), load-bearing-guards comment in the hook, "no fetch tool at all → SKIP" clause + alignment glitch in update-check.md, and the upgrade.md in-section-field propagation caveat (minor #5: new Part 2 rows like "Pack source" won't survive a future upgrade's ##-level reconcile unless flagged). Skipped 1 cosmetic doc nit.
- State: self-checks PASS — version v12.8 (no stray v12.7 outside pack-dev), ls-vs-index 31=31 bidirectional, hook bash -n clean + default no-op verified, cross-refs resolve. Hook marked executable (git +x).
- Watch: carried OPEN — KEY VALIDATION (real-12B LEAN), upgrade.md e2e dry-run, model-tiering live dogfood (predefined light-tier OpenCode/Codex subagent), PROBE 2/3. NEW: the notify-hook's live behavior (SessionStart stdout injection + real fetch against a configured PACK_SOURCE_URL) is unexercised — verify on a real Claude Code launch once a distribution URL exists. Also: upgrade.md now FLAGS new in-section Part 2 fields but does not auto-add them — a fuller row-level reconcile is a future option if it recurs.

## [2026-06-15] Ship Light-tier subagent templates (3 harnesses) + activation steps + v12.9 — Claude Code
- Trigger: user's live OpenCode correctly diagnosed that v12.7 fixed the POLICY but the practical blocker is now config + harness exposure — OpenCode/Codex need a predefined named subagent pinned to a real Light model, plus restart, plus description-driven invocation (no per-call selector). User chose "ship templates (all 3) + steps."
- Did: shipped three fill-the-model Light-tier subagent templates, all inert until renamed (the .example suffix means no harness loads them): .claude/agents/light-checker.md.example (model: haiku — already valid, works on rename with no model edit; tools: Read,Grep,Glob = read-only), .opencode/agent/light-checker.md.example (mode: subagent, model: REPLACE_WITH/your-light-model-id, permission: edit deny), .codex/agents/light-checker.toml.example (name/description/developer_instructions + model placeholder). Each scoped by description to bounded rubric/checklist work ONLY and explicitly excludes review/security/judgement (Capable-tier), with a hand-back-on-ambiguity instruction. Added model-tiering.md "### Predefined Light-tier subagents (shipped templates)" — the per-file model/restart summary + the 4 activation steps (choose+record model → drop .example + set model → RESTART OpenCode/Codex (load at startup) / fresh session for Claude → confirm invocation via @mention/description-match, NOT a by-name model tool; fail-safe = can't invoke → run Capable, never skip). Tier-map bullet now points to the templates. Headers v12.8→v12.9 (incl. the update-check hook).
- Decisions: ship as .example (inert) not live — WHY: a placeholder model string would error if a harness loaded it at startup; .example guarantees nothing activates just by installing the pack (same opt-in ethos as the notify-hook). Activation note lives as a YAML/TOML `#` comment INSIDE the frontmatter (not an HTML comment before `---`) — WHY: keeps the file frontmatter-first and copy-ready, so renaming yields a valid agent file. Claude template ships with a working model (haiku) but OpenCode/Codex can't (no universal valid default) — WHY: matches each provider's reality; the pack provides the artifact, the user provides the one model string only they know.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE-WITH-MINORS, 0 blockers. Verified inertness (only .example on disk; .md.example is not a .md file Claude loads), schema validity per harness, read-only/non-judgement scoping, and that the 4 steps don't overclaim (no per-call promise for OpenCode/Codex; "fresh session" not "no restart" for Claude). Fixed 1 minor (Codex header parity: noted inert-until-renamed + model optional); 3 cosmetic minors acknowledged, not actioned (OpenCode placeholder slash; Claude name-from-filename; model-optional wording) — all trivial, none affect correctness.
- State: self-checks PASS — version v12.9 (no stray v12.8 outside pack-dev), ls-vs-index 31=31, all three templates parse (YAML+TOML validated). The OpenCode tiering chain is now complete on the pack side: policy (v12.7) + never-blank fill (v12.7) + shipped artifact + activation steps (v12.9). Remaining is purely user-side: pick the model string, drop .example, restart.
- Watch: carried OPEN — KEY VALIDATION (real-12B LEAN), upgrade.md e2e dry-run, update-check notify-hook live-fire, PROBE 2/3, SETUP Step 0 URL. UPDATED: the model-tiering live dogfood is now concretely "activate light-checker.md in OpenCode with a real model, restart, confirm the primary delegates a bounded scan to it" — the templates + steps exist; only a live OpenCode/Codex session with a chosen model can close it.

## [2026-06-15] Agent-driven Light-tier activation (no user file-editing) + v12.10 — Claude Code
- Trigger: user — the harness/agent should PROMPT the user to pick the model, then ACTIVATE it and INDICATE if a restart is needed, "rather than the user editing .md files directly." v12.9 left activation as a manual user step (drop .example, edit model); v12.10 makes the agent do the file write.
- Did: reworked model-tiering.md so tiering setup is agent-driven. New tier-map bullet: "When a Light model is chosen, the agent activates it — it does not stop at recording the string" (writes the live agent file from the template, then signals restart; one continuous setup step; also applies when a single-tier project later turns tiering on). Rewrote the activation block into a 5-step agent-driven flow: PROMPT (propose Light+Capable, ask once; skip/one-model → single-tier) → RECORD in Part 2 map → ACTIVATE (the AGENT copies light-checker.*.example → live name + sets model; "the user does not open or edit the file") → INDICATE RESTART (per-harness matrix: OpenCode restart / Codex restart / Claude no harness restart but new session) → CONFIRM invocation (fail-safe unchanged). Headers v12.9→v12.10 (core + 3 templates + hook).
- Decisions: agent writes the live config file, user only decides (model) + acts where only they can (restart) — WHY (user): non-devs (and devs) shouldn't hand-edit YAML/TOML agent files; the decision is the model, the rest is mechanical and the agent's job. "Indicate restart" is explicit per-harness — WHY: the new sub-agent is invisible until the harness reloads and the agent cannot restart its own harness, so silence would look like a broken setup. Claude wording tightened to "appears only in a NEW session, not the current one" — WHY (review minor): prevents a cold agent saying "no restart" then trying to invoke it same-session.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE-WITH-MINORS, 0 blockers. Confirmed: 5-step block and tier-map bullet agree; restart matrix matches each template header; no overclaim (Claude "new session" not mid-session); guardrail OK (a project-level .opencode/.claude/.codex agent file written during user-confirmed setup is NOT a hard-guardrailed pack-instruction file — those are only AGENTS.md Part 1/CLAUDE.md/TASK_TEMPLATE.md/protocols/**); no fabricate-a-model or skip-the-prompt path. Fixed 1 minor (Claude same-session wording); 3 cosmetic minors not actioned.
- State: self-checks PASS — v12.10 (no stray v12.9 outside pack-dev), ls-vs-index 31=31, templates still parse. OpenCode tiering UX is now: agent asks → agent writes the file → agent says "restart OpenCode." User never edits a file.
- Watch: carried OPEN — tiering live dogfood (now: confirm the agent-driven activation + restart prompt actually produces a working delegating Light agent in a live OpenCode/Codex session), KEY VALIDATION real-12B LEAN, update-check notify-hook live-fire, upgrade.md e2e, PROBE 2/3, SETUP Step 0 URL.

## [2026-06-15] Bake canonical pack source into the pack (single source of truth) + v12.11 — Claude Code
- Trigger: user — "the pack source needs to be explicitly set in the pack so agents know where to look for updates." It shipped [NOT SET], so update-check couldn't run out of the box.
- Verified first: WebFetch of https://raw.githubusercontent.com/LarsonRogers/AI_Agent_Starter_Pack/main/AGENTS.md returns the live AGENTS.md with its version header — the canonical upstream is reachable and the mechanism works against it (public repo).
- Did: set AGENTS.md Part 2 → Related Docs → "Pack source" row to that canonical raw URL and declared it THE single source of truth (the URL lives in exactly one place in the pack). Reworked the notify-hook (.claude/hooks/check-pack-update.sh) to READ the URL from the local AGENTS.md via grep instead of a hardcoded PACK_SOURCE_URL — so baking the URL into AGENTS.md makes both the on-demand check and the hook work with zero script edits. Updated update-check.md ("ships pre-set; one field, both consumers read it; fork changes that one URL; blanked → say so, don't guess") and its hook section ("nothing to edit in the script — just register it"). SETUP Step 0: filled the human distribution link to the GitHub repo and reframed update-checks as "work out of the box" (fork → change both the repo link and the Pack source row). Headers v12.10→v12.11.
- Decisions: single field = single source of truth; hook greps it from AGENTS.md, doesn't duplicate — WHY: two copies (protocol + script) would drift; one field read by both consumers can't. Canonical URL baked as the shipped default, overridable by a fork in that one row — WHY: the pack has one known home, so update-check should work with zero setup; "do not guess a URL" (the earlier rule) was about an AGENT inventing one, not the maintainer explicitly setting it.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers, no material minors. Confirmed: URL in exactly one place (AGENTS.md:611), grep class [^ )|`<>]+ stops at the closing backtick (no trailing-backtick capture) and no http-bearing "pack source" line exists above row 611 (two confirming greps), every hook exit path returns 0 and writes nothing, detect stays separate from apply, URL well-formed for branch main. Latent (non-defect) note: head -1 + document order would matter only if someone later adds an http-bearing "pack source" line above row 611.
- Validated locally: URL extraction from AGENTS.md returns the clean URL (no backtick); hook bash -n clean. NOT validated here: the hook's live curl to GitHub — outbound network from the hook is blocked in this sandbox (the command was denied). The URL is WebFetch-confirmed reachable, the compare logic is reviewer-verified, but the hook's own fetch path stays a live-fire watch item.
- State: self-checks PASS — v12.11 (no stray v12.10 outside pack-dev), ls-vs-index 31=31. Update-check now works out of the box: agents know where to look (AGENTS.md Pack source), zero setup.
- Watch: carried OPEN — notify-hook live-fire (now: confirm the hook's curl actually fetches the baked-in URL on a real Claude Code launch and notifies when behind — partially de-risked: URL-extraction + reachability + compare all verified, only the in-hook fetch unexercised), tiering live dogfood, KEY VALIDATION real-12B LEAN, upgrade.md e2e, PROBE 2/3. RESOLVED-ish: SETUP Step 0 distribution link now filled (GitHub repo) + Pack source set.

## [2026-06-15] Proactive session-start tier-map offer (ask-once) + v12.12 — Claude Code
- Trigger: OpenCode (in a separate project) still sat single-tier and never spawned lower-tier sub-agents until the user explicitly asked. Root cause (confirmed by exploration): the tier-map setup flow lives only in on-demand model-tiering.md, which fires only at First-Session/Inherited onboarding or when a delegation is imminent — a plain resumption session never triggers it, so the map stays at placeholders silently. The activation machinery (v12.10 5-step flow + shipped templates) was complete; the missing piece was a PROACTIVE trigger. (Plan written + approved before edits.)
- Did: added an always-on, ask-once "Tier-map offer" to AGENTS.md Session Start, piggybacked on the existing Pack-profile read of Part 2 → Model Tiers — if the Light row is still a bare [placeholder], offer ONCE to set up tiering and load model-tiering.md if accepted; resolved (real model OR "none — single-tier (decided YYYY-MM-DD)") → silent; skip read-only. Defined "Resolved" once in the Part 2 Model Tiers comment (single source of the test) and made the declined-sentinel literal explicit in the Light row placeholder. model-tiering.md: never-blank bullet now writes the dated sentinel on decline (durable recorded-state anti-nag, not memory); added a line that the live agent file is project config → commit it (only the pack-dev repo excludes its dogfood copy via .git/info/exclude, never shipped). Headers v12.11→v12.12.
- Validation context: the user's live OpenCode independently ran the v12.10 agent-driven activation end-to-end in their separate project (created .opencode/agent/light-checker.md, set openai/gpt-4.1-mini, updated Model Tiers, flagged restart) — confirms the activation machinery works in real OpenCode; this change makes it fire proactively instead of only when asked.
- Decisions: trigger in always-on AGENTS.md, not session-start.md — WHY: session-start.md is on-demand and skips type-C/D/resumption/read-only, exactly the sessions that currently fail; the Pack-profile read is already always-on, so one clause piggybacks for ~8 lines and fires in all 3 harnesses. Anti-nag = recorded state, not memory — WHY: a session has no memory of prior offers; keying off a bracket-placeholder vs a written sentinel makes "ask once" durable. "Resolved" defined in exactly one place — WHY (review risk): sentinel drift between the test and the write would re-nag forever.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers. Confirmed sentinel byte-identical across all 4 sites, read-only suppressed (belt-and-suspenders with the meta-review preemption), no duplicated procedure (trigger here, 5-step flow stays in model-tiering.md), philosophy preserved (single-tier always valid), parity holds, commit-note not inverted (deployed projects commit; only pack-dev excludes). Fixed 1 minor (trigger now keys off a bare [placeholder] matching the authoritative Part 2 definition, not an abbreviated quote); 1 cosmetic minor skipped.
- State: self-checks PASS — v12.12 (no stray v12.11 outside pack-dev), ls-vs-index 31=31, sentinel literal consistent. Always-on floor +~8 lines (a trigger, not a procedure) — logged as a deliberate, bug-justified exception to the LEAN-floor discipline.
- Watch: carried OPEN — tiering POST-RESTART delegation-actually-routes confirmation (activation now confirmed live in OpenCode; only "does the primary delegate to Light after restart" remains, user is eyeballing), KEY VALIDATION real-12B LEAN, notify-hook live-fire, upgrade.md e2e, PROBE 2/3. Accepted wart: pack-dev repo's own template ships placeholders, so the offer fires every pack-dev session and can't be resolved without polluting the template — maintainer just declines.

## [2026-06-15] Note access-method-vs-provider caveat in model tiering + v12.13 — Claude Code
- Trigger: user — the available model IDs depend on the ACCESS METHOD, not just the provider: OpenAI via online/subscription login (OAuth) exposes a different model set than via API key, and this likely generalizes across providers. An agent proposing a Light model could pick an ID the user's actual auth can't call.
- Did: added the caveat to model-tiering.md's "Provider / environment" axis bullet — the same provider can expose a different usable model-ID set by access method (login/OAuth vs API key vs gateway); treat available models as a function of provider AND auth path; when proposing a Light model, offer/confirm one the user's actual access path exposes, or ask which cheaper model their login/key/gateway offers, rather than assuming a brand-name string. Updated the Part 2 → Model Tiers "Provider / environment" placeholder to "provider AND access method (available model IDs can depend on both)" with login-vs-API-key examples. Headers v12.12→v12.13.
- Decisions: hedge the generalization ("likely holds across providers"), anchor on the concrete observed case (OpenAI login vs API key) — WHY: it's a real observation, not a verified universal law; overclaiming would be dishonest. Caveat lives in the Provider axis only (single-sourced) — WHY: access method is a property of "which models exist," and duplication drifts.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers. Confirmed accurate+hedged, actionable (propose/confirm/ask triad, not just a warning), consistent with detect→propose→ask-once and the "never guess a cheaper model → degrade to Capable" fail-safe (the caveat reinforces it), tight/no-duplication, parity intact (provider-neutral prose, no harness path). Fixed 1 minor (softened the Part 2 placeholder "depend" → "can depend" for tonal parity with the protocol's hedge). Could-not-verify: the external empirical fact itself (out of scope; text marks it observed-and-likely).
- State: self-checks PASS — v12.13 (no stray v12.12 outside pack-dev), ls-vs-index 31=31.
- Watch: unchanged from v12.12 — tiering post-restart delegation confirmation, notify-hook live-fire, KEY VALIDATION real-12B LEAN, upgrade.md e2e, PROBE 2/3.

## [2026-06-16] Requirement Pressure-Test: shared interrogation before build/change + v12.14 — Claude Code
- Brief (confirmed, cross-cutting pre-flight approved): onboarding under-interrogates requirements — product-definition Step 1 stops at "can I write the brief," never pressure-tests assumptions/edges/failure-modes. User asked whether to use grill-me; decided to adopt the TECHNIQUE not the Claude-only skill (tri-harness parity), as one shared protocol invoked from three callers with a per-context lens + per-context gate. Design choices confirmed: one shared protocol (not 3 copies); no new always-on Standing Rule; inherited fires AFTER assessment; task-brief gated (big/ambiguous/cross-cutting), never universal.
- Did: new protocols/requirements.md ("Requirement Pressure-Test") — portable equivalent of an adversarial requirements grilling, explicitly NOT a dependency on the grill-me skill. Technique = interrogate assumptions/edges/failure-modes/conflicts/hidden-dependencies/concrete-success, each question carries its "why"; bounded + audience-scaled (Developer harder/technical, Non-dev fewer/plainer/one-at-a-time); stopping rule = risky unknowns resolved (not a count); anti-nag = don't manufacture questions, zero questions is valid; output = resolved unknowns feed the brief, accepted risks recorded as stated assumptions/watch items. Three lenses: PRODUCT / CHANGE BLAST-RADIUS / SCOPE & ACCEPTANCE. Wired: product-definition Step 1b (PRODUCT, always) + tightened Step 1 stopping rule + Done line; inherited-codebase Phase 2b (BLAST-RADIUS, after the Phase 2 assessment so it's informed by the map); cross-cutting "pressure-test the requirement first" note (SCOPE, before the mechanical pre-flight plan); AGENTS.md Task-Brief clause (always-on hook so ambiguous SMALL briefs are covered too) + Protocol Index row (31→32). Headers v12.13→v12.14.
- Decisions: one shared protocol, lens chosen by caller — WHY: anti-duplication/anti-drift, pack precedent (session-start/task-workflow extraction); three embedded copies would diverge. Inherited interrogation AFTER assessment — WHY: the code map makes the edge/failure/blast-radius questions concrete instead of generic. Task-brief pressure-test gated, never universal — WHY: grilling a one-line fix is the nag failure mode; reuse the existing big/ambiguous/cross-cutting threshold; the universal reformulation still runs for small briefs. Composes with, never replaces, the task-brief reformulation and the inherited Phase-2 "unknown areas" item.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers, no material minors (2 cosmetic casing/wording). Verified all 6 axes: tri-harness parity (no skill dependency; grill-me named once only to disclaim it), anti-nag/gating per caller, composition-not-duplication (no double-interrogation path; reformulation untouched), lens correctness (inherited = blast-radius not product), ls-vs-index 32=32 both ways + 4 caller refs resolve, no guardrail touched + no new Standing Rule + read-only excluded.
- State: self-checks PASS — v12.14 (no stray v12.13 outside pack-dev), protocols 31→32, ls-vs-index 32=32, all cross-refs resolve. Always-on floor +~3 lines (one Task-Brief clause), no new Standing Rule.
- Watch: carried OPEN — tiering post-restart delegation confirmation, notify-hook live-fire, KEY VALIDATION real-12B LEAN, upgrade.md e2e, PROBE 2/3. NEW: the pressure-test is prose-verified only — no live run yet against a real vague idea / inherited change / big task brief to confirm it surfaces real unknowns without nagging (trial-phase candidate, especially the Non-dev audience-scaling).

## [2026-06-16] Project Stakes dial — scale process ceremony to project stakes + v12.15 — Claude Code
- Trigger: the A/B probe (pack-dev/ab-test-pack-value.md) showed the pack applies full production ceremony (CI+dependabot+semgrep, 5 docs, 18 tests, ~2x tokens) uniformly — heavy for a throwaway. The pack sizes architecture (S1–S4) and context (FULL/LEAN) but not its own process ceremony. Recommendation accepted: a "safe minimization" that gates ceremony, removes nothing.
- Did: new protocols/project-stakes.md — a third dial (Spike / Standard / Production) that scales enforcement-tooling bundle + doc set + test depth + demo formality, NEVER the safety floor. Floor (every posture incl. Spike): secrets + the pre-commit secret hook (enforcement-tooling gate 5), secure-coding self-check on input/auth/data, independent review when triggered, the day-one architecture sketch, don't-commit-broken-state + one log entry. Default = Standard (≈ current behavior); Spike/Production proposed+confirmed, never auto-selected. Spike auto-escalates UP (logged) on real-data/auth/deploy/people-relying; down only by explicit logged user decision. Wired (7 consumers): AGENTS DoD qualifier + Part 2 "Project Stakes" field + Protocol Index row (32→33); product-definition Step 3d (propose+set) + stakes-scaled backlog item 1 + Done line; inherited-codebase Phase 3 step 4d; enforcement-tooling "scale the bundle" block (gate 5 flagged FLOOR-always); run-demo + testing-strategy scaling notes (with critical-path-forces-escalation exception); placeholder-inference registration. Headers v12.14→v12.15.
- Decisions: scales ceremony, never the floor — WHY: same principle as LEAN ("trim footprint, not discipline"); a safe minimization that removes no capability and relaxes no guardrail. Default Standard + propose-don't-auto-select — WHY: no existing project silently loses rigor; the saving comes only when a throwaway is declared Spike. Escalation up-only + auto — WHY: a spike that grows real data/auth/deploy must ratchet up so it can't quietly become a real system at spike rigor.
- Review: independent fresh-context reviewer (Capable tier) — APPROVE-WITH-MINORS, 0 blockers. Verified floor never scaled (identical across project-stakes/DoD/enforcement-tooling), no silent downgrade, escalation unbypassable+up-only, philosophy fits, ls-vs-index 33=33, v12.15 consistent. Fixed all 3 minors: (M1) secure-coding.md + AGENTS standing rule said "SAST in CI" unconditionally, contradicting SAST=Production-only → reconciled (SAST per stakes, Production default, pulled forward for auth/payments/sensitive-data; the self-check stays floor); (M2) gate 7 atomic (CI+dependabot) vs tables splitting dependabot to Production → clarified Standard = CI-wiring-only, dependabot is the Production half; (M3) table "+"-delta could read secret-hook as Spike-only → added a cumulative-columns + secret-hook-is-floor note.
- State: self-checks PASS — v12.15, protocols 32→33, ls-vs-index 33=33, all 7 consumer cross-refs resolve. This is the "safe" minimization; deeper content trims (security-checklist/meta-doc weight) still deferred to the second A/B pair's security/relational run.
- Watch: carried OPEN — second A/B pair (relational + login feature, to inform content-level trims and fight N=1), pressure-test live trial, tiering post-restart, notify-hook live-fire, KEY VALIDATION real-12B LEAN, upgrade.md e2e, PROBE 2/3. The scratch probe repos are at I:/mega/megasync/projects/pack-ab-probe (disposable).

## [2026-06-16] A/B probe-2 (security) results recorded + re-weight secure-coding to high-miss items + v12.16 — Claude Code
- (a) Recorded probe results in pack-dev/ab-test-pack-value.md (Results — run 1): architecture (Arm A SQL inline in routes / Arm B data layer — anecdote reproduced; retrofit closer than expected; pack ~2x tokens → drove v12.15 Project Stakes) and security (login + author-only edit/delete: BOTH got IDOR/ownership right unprompted; the DIVERGENCE was CSRF — Arm A shipped login/register/edit/delete with no CSRF token and rationalized it; Arm B has full CSRF because the pack's independent-review gate caught it as a BLOCKER + closed a username-enumeration timing channel; Arm A 0 tests / Arm B 34). Headline: pack's win is the review gate catching a real, shippable, NON-OBVIOUS vuln the unguided model wrote and rationalized — not "writes more secure code."
- (b) Re-weighted protocols/secure-coding.md (NOT trimmed — all 23 checklist items kept): added a "Where capable models actually miss — verify these hardest" block at the top of the checklist (CSRF incl. login-CSRF/session-fixation; sessions invalidate/expire/rotate + rate-limit; authz on EVERY endpoint + object ownership; enumeration/timing/error leak), explicitly a front-loaded SUBSET ("do not skip the rest"); reframed the basics (parameterization/escaping/IDOR) as "capable model usually handles — confirm present, don't belabor". Headers v12.15→v12.16.
- Decisions: emphasis, not deletion — WHY: the probe shows capable models do the basics + IDOR unprompted but miss CSRF/session/enumeration, so direct the scrutiny there; but the full checklist stays complete because weak/local models (a pack target) are NOT reliable on the basics. Keep the independent-review + secure-coding self-check gates — WHY: they caught the live CSRF vuln; this resolves the deferred "maybe trim the security checklist" question with evidence (don't).
- Review: independent fresh-context reviewer (Capable tier) — APPROVE, 0 blockers. Verified no item deleted/skippable, high-miss block is an accurate additive subset, self-check gate + floor status intact, consistent with project-stakes.md (self-check = floor) + the SAST-per-stakes wording, no overclaim (basics still must be confirmed present). Fixed both minors: (M1) tied the "don't belabor basics" relaxation to capable models only — weak/local models apply the basics in full; (M2) gave the enumeration/timing high-miss item a home in the detailed Errors & logging section.
- State: self-checks PASS — v12.16, ls-vs-index 33=33, all 23 checklist items present (re-weight not trim).
- Watch: carried OPEN — option (c) a FRESH replication pair to fight N=1 (maintainer deciding); pressure-test/stakes live trials; tiering post-restart; notify-hook live-fire; KEY VALIDATION 12B LEAN; upgrade.md e2e; PROBE 2/3. Probe scratch repos at I:/mega/megasync/projects/pack-ab-probe (disposable).
